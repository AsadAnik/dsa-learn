/**
 * FIND LARGE CONTIGUOS SUM IN AN ARRAY
 * @param arr 
 * @returns 
 */
function findLargeContiguosSum(arr: number[]): number {
    const arrSize: number = arr.length;
    console.log(arrSize, arr);
    return 0;
}

// Test here
const resultContiguosSum: number = findLargeContiguosSum([1, -2, 3, 4, -1, 2]);
console.log('Found Contiguos Sum - ', resultContiguosSum);